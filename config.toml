baseurl = "/"
title = "GanniPiece Blog"
languageCode = "zh-tw"
paginate = "10" # Number of posts per page
theme = "mainroad"
disqusShortname = "" # Enable Disqus comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id

[Author] # Used in authorbox
  name = "GanniPiece"
  bio = "To share what I've learned."
  avatar = "img/avatar.png"

[Params]
  description = "GanniPiece Personal blog about everything" # Site description. Used in meta description
  copyright = "GanniPiece" # Footer copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  schema = true # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  authorbox = true # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = ["author", "date", "categories", "translations"] # Order of post meta information
  mainSections = ["posts", "categories"] # Specify section pages to show on home page and the "Recent articles" widget
  readmore = true # Show "Read more" button in list if true
  dateformat = "2006-01-02" # Change the format of dates
  mathjax = true # Enable MathJax
  mathjaxPath = "https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.6/MathJax.js" # Specify MathJax path
  mathjaxConfig = "TeX-AMS-MML_HTMLorMML" # Specify MathJax config
  googleFontsLink = "https://fonts.googleapis.com/css?family=Open+Sans:400,400i,700" # Load Google Fonts
  customCSS = ["css/custom.css"] # Include custom CSS files
  customJS = ["js/custom.js"] # Include custom JS files

  # DEPRECATED PARAMS
  subtitle = "" # Deprecated in favor of .Site.Params.logo.subtitle
  highlightColor = "" # Deprecated in favor of .Site.Params.style.vars.highlightColor

[Params.style.vars]
  highlightColor = "#e22d30" # Override highlight color
  # Override font-family sets. Secondary font-family set responsible for pre, code, kbd, and samp tags font
  # Take care of different quotes OR escaping symbols in these params if necessary
  fontFamilyPrimary = "'Open Sans', Helvetica, Arial, sans-serif"
  fontFamilySecondary = "SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"

[Params.logo]
  image = "img/placeholder.png" # Logo image. Path relative to "static"
  title = "GanniPiece" # Logo title, otherwise will use site title
  subtitle = "" # Logo subtitle

[Params.thumbnail]
  visibility = ["list", "posts"] # Control thumbnail visibility

[Params.sidebar]
  # true, false, left
  home = true # Configure layout for home page
  list = true  # Configure layout for list pages
  single = true # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social", "languages"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  categories_counter = true # Enable counter for each category in "Categories" widget
  tags_counter = true # Enable counter for each tag in "Tags" widget

[Params.widgets.social]
  # Enable parts of social widget
  facebook = "ginedoc"
  twitter = "bear110088"
  # linkedin = "username"
  github = "gannipiece"
  gitlab = "bear110088"
  email = "bear110088@gmail.com"

[menu]
	[[menu.main]]
		identifier = "/"
		name = "所有發布"
		url = "/"
		weight = 10
	[[menu.main]]
		identifier = "categories"
		name = "所有分類"
		url = "/categories/"
		weight = 20
	[[menu.main]]
		identifier = "tags"
		name = "所有標籤"
		url = "/categories/"
		weight = 30
	[[menu.main]]
		identifier = "about"
		name = "關於我"
		url = "/about/"
		weight = 40
